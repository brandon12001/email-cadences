APP_PASSWORD = "choose-a-password"
ANTHROPIC_API_KEY = "sk-ant-your-key"
ANTHROPIC_MODEL = "claude-haiku-4-5"
ANTHROPIC_WEB_SEARCH = "true"

# There is no SMTP configuration. The app never sends email.
# Sending happens in Word and Outlook via Cadence_Sender_Macro.vba.txt,
# which is what applies the Salesforce BCC and your real signature.
